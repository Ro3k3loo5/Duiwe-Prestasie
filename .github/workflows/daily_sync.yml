name: Continuous Pigeon Sync

on:
  workflow_dispatch: {}
  schedule:
    # This instructs GitHub to run the sync pipeline every 5 minutes automatically
    - cron: '*/5 * * * *'

permissions:
  contents: write

jobs:
  build-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Data Tools
        run: |
          pip install pandas openpyxl beautifulsoup4 requests

      - name: Run Scraper Engine
        run: python scraper.py

      - name: Save Changes to Repository
        run: |
          git config --global user.name "Pigeon Automated Bot"
          git config --global user.email "bot@github.com"
          git add data/
          git commit -m "Automated Sync: Multi-matrix live refresh" || echo "No changes to commit"
          git fetch origin main
          git rebase origin/main || { echo "Rebase failed — aborting"; git rebase --abort || true; exit 1; }
          git push origin HEAD:main
