name: Hourly Pigeon Sync Engine

on:
  schedule:
    - cron: '0 * * * *' # Executes exactly at the top of every hour
  workflow_dispatch: # Allows you to trigger an instant update manually via the GitHub dashboard

jobs:
  sync-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Crucial permission flag allowing the runner to save files back to your repository

    steps:
    - name: Checkout Code Repository
      uses: actions/checkout@v4

    - name: Set Up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Architecture Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas gspread google-auth

    - name: Execute Live Scraper Data Extraction
      env:
        GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
      run: python scraper.py

    - name: Commit and Push Updated Cache Ledger Files
      run: |
        git config --global user.name "GitHub Automation Bot"
        git config --global user.email "actions@github.com"
        
        # Stage only the generated data files
        git add data/*.json
        
        # Commit only if files have changed to prevent unnecessary build cycles
        if git diff --staged --quiet; then
          echo "No new racing developments tracked. Ledger up to date."
        else
          git commit -m "Automated data sync update [Newest First Sort]"
          git push
        fi
